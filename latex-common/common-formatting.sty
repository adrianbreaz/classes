% common sense
\usepackage[letterpaper]{geometry}

% good margins
\geometry{top=1.0in, bottom=1.0in, left=1.0in, right=1.0in}

% pretty links
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    urlcolor=blue,
    citecolor=black,
    linkcolor=black
}

% first paragraph after section indentat
% \usepackage{firstindent}

% no silly paragraph indentation
\setlength{\parindent}{0in}
\setlength{\parskip}{0.1in}

% number pages
\pagenumbering{arabic}